<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
    <title>Shop</title>
    <script src="/public/script.js"></script>
    <style>
    </style>
</head>
<body style="background-color: #eee;">
    <div class="container">
        <div class="row">
            <div class="col-1 col-lg-2 py-4 my-2">
                {{#if user.business}}
                    <a href="/add" class="h5 text-dark nav-link" style="outline: inherit;">Add Product</a>
                    <!--! not working -->
                {{/if}}
            </div>
            <div class="col-8 text-right col-lg-5">
                <h1 class="display-4 my-lg-4 mt-4"><b>SHOP</b></h1>
            </div>
            <div class="col-10 py-lg-4 my-lg-2 col-lg-5 mx-0 d-flex justify-content-end">
                {{#if user}}
                    <a href="/cart" class="h5 text-dark ml-1 nav-link" id="cart" style="outline: inherit;">Cart</a>
                    {{else}}
                    <a href="/cart" class="h5 text-dark ml-1 disabled nav-link text-muted" id="cart" style="outline: inherit;">Cart</a>
                {{/if}}
                {{#if user}}
                    <form action="/logout" method="POST">
                        <button type="submit" class="h5 text-dark ml-1 nav-link" style="border: none;">Logout</button>
                    </form>    
                    {{else}}
                    <a href="/register" class="h5 text-dark ml-1 nav-link" id="cart" style="outline: inherit;">Signup</a>
                    <a href="/login" class="h5 text-dark ml-1 nav-link" id="cart" style="outline: inherit;">Login</a>
                {{/if}}
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row" id="productlist">
            {{#each products as |p|}}
                <div class="col-lg-3 col-sm-6 my-3">
                    <div class="card">
                        <div class="card-body">
                            <h3 class="p-0 m-0 text-capitalize">{{p.name}}</h3>
                            <h7 class="text-muted text-capitalize">{{p.manufacturer}}</h7>
                            <hr>
                            <form action="/cart" method="post">
                                <div class="row mt-5">
                                    <div class="col py-2">{{p.price}}</div>
                                    <button class="btn btn-info col mr-3" value="{{p.id}}" name="id">Buy</button>
                                </div>    
                            </form>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
        {{!-- <a href="/test">test</a> --}}
    </body>
    </html>